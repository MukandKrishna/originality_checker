# Core (required)
numpy>=1.24
scikit-learn>=1.3
rapidfuzz>=3.0

# Document parsing (optional)
pypdf>=4.0
python-docx>=1.1

# Semantic similarity + FAISS (optional but recommended)
sentence-transformers>=3.0
faiss-cpu>=1.8

# The default embedding model is:
#   sentence-transformers/all-mpnet-base-v2  (768-dim, best quality of all-* family)
# For maximum speed use:
#   sentence-transformers/all-MiniLM-L6-v2   (384-dim, ~5x faster)
# For multilingual use:
#   BAAI/bge-m3  (requires threshold recalibration) or intfloat/multilingual-e5-large
